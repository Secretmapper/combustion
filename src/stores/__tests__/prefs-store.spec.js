import React from 'react';
import PrefsStore from '../prefs-store'

describe('PrefsStore', () => {
  describe('hydrates', () => {
    xit('default values', () => {})
    xit('default values missing', () => {})
    xit('statusFilter properly', () => {})
  })
  xit('persists values properly', () => {})
})
